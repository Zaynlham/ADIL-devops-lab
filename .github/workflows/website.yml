name: Website CI

on:
  push:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Verify index.html exists
        run: test -f index.html

      - name: Show website title
        run: grep "<title>" index.html
